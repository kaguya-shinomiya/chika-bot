query SearchAnime($search: String, $type: MediaType) {
  Media(search: $search, type: $type) {
    title {
      userPreferred
    }
    description(asHtml: false)
    season
    seasonYear
    status
    averageScore
    episodes
    duration
    coverImage {
      medium
    }
    genres
  }
}
